tasks:
- id: ci-005-openapi-spec
  title: Seed OpenAPI 3.1 spec as single source of truth
  criteria:
  - OpenAPI spec exists at docs/openapi.yaml with version 0.1.0
  - 'All five operation groups present: Health, Relay, Registry, Inbox, Ack'
  - 'Relay: publish, subscribe (WebSocket upgrade), list topics endpoints defined'
  - 'Registry: CRUD for agents with capabilities and public_key fields'
  - 'Inbox: deliver, retrieve (with lease), ack endpoints defined per agent'
  status: complete
  created_at: '2026-07-12T01:53:21.840145+00:00'
  completed_at: '2026-07-12T01:57:05.903962+00:00'
- id: ci-001-relay-server
  title: Wire relay server with HTTP pub/sub and WebSocket topic routing
  criteria:
  - POST /relay/publish endpoint exists and accepts JSON with topic and event fields
    returning 202
  - GET /relay/subscribe/{topic} endpoint exists with WebSocket upgrade returning
    101
  - GET /relay/topics endpoint exists returning 200 with active topics list
  - internal/relay/ package has Relay struct with Publish, Subscribe, and Topics methods
  - "internal/relay/ has in-memory topic routing with subscriber channels \u2014 no\
    \ external dependencies"
  - cmd/server/main.go registers all three relay routes on the mux.Router
  - All relay code has unit tests in internal/relay/ with >80% coverage
  status: complete
  created_at: '2026-07-12T01:57:39.266387+00:00'
  completed_at: '2026-07-12T02:08:34.415723+00:00'
- id: ci-002-mesh
  title: 'CI-002: Port mesh + peer connection from Hivemind'
  criteria:
  - PeerConnection.Connect() dials WebSocket, starts read loop
  - PeerConnection.Send() writes with 10s deadline
  - PeerConnection.Close() sends close frame, cleans up
  - "Mesh.ConnectPeer() dials \u2192 registers \u2192 starts keepalive"
  - "Mesh.SendRequest() marshals \u2192 sends \u2192 waits for response with timeout"
  - Keepalive ticker fires every 30s, sends KEEPALIVE message
  - handleMessage() dispatches RESPONSE to pending channel, ERROR to error channel
  - Unit tests for message marshal/unmarshal round-trip
  status: complete
  created_at: '2026-07-12T14:00:39.990133+00:00'
  completed_at: '2026-07-12T14:07:28.177942+00:00'
- id: ci-003-agent-registry
  title: 'CI-003: Agent registry + persistent inboxes with lease-based delivery'
  criteria:
  - POST /agents registers agent with ed25519 public key returning 201 and 409 for
    duplicates
  - GET /agents lists all registered agents, GET /agents/{id} returns detail with
    health status
  - DELETE /agents/{id} unregisters agent and cleans up its inbox
  - POST /agents/{id}/inbox delivers a message to agent's FIFO inbox
  - "GET /agents/{id}/inbox retrieves leased messages \u2014 only un-ACKed messages\
    \ returned, concurrent retrievers get disjoint sets"
  - POST /agents/{id}/inbox/ack acknowledges messages permanently removing them; un-ACKed
    messages return to queue after lease TTL expires
  - TTL-expired messages auto-purged from inbox
  - internal/registry/ package has unit tests with >80% coverage covering all 8 acceptance
    criteria from spec
  status: complete
  created_at: '2026-07-14T06:20:27.404197+00:00'
  completed_at: '2026-07-14T06:45:15.143493+00:00'
- id: ci-004-wire-api
  title: Wire full HTTP API surface with middleware, mesh handler, and graceful shutdown
  criteria:
  - Mesh handler accepts WebSocket connections at /mesh/connect/{agentID}
  - GET /mesh/peers returns connected peer list with count
  - 'Middleware: Logging logs method, path, status, duration for every request'
  - 'Middleware: Recovery catches panics and returns 500'
  - main.go wires all 15+ endpoints across relay, mesh, registry, and inbox packages
  - Graceful shutdown calls meshSvc.Stop() before server.Shutdown()
  - Ticker-based PurgeExpired runs every 30s in background goroutine
  status: complete
  created_at: '2026-07-14T14:04:54.096317+00:00'
  completed_at: '2026-07-15T22:20:50.628340+00:00'
- id: feat-002
  title: 'FEAT-002: Structured logging with log/slog'
  criteria:
  - config/config.go has LogLevel and LogFormat fields parsed from CR_LOG_LEVEL/CR_LOG_FORMAT
    env vars
  - cmd/server/logger.go creates initLogger() that sets slog.SetDefault() with configurable
    level and format
  - cmd/server/main.go calls initLogger() and uses slog instead of log
  - cmd/crier-mcp/main.go uses slog instead of log
  - internal/middleware/middleware.go uses slog.Info/slog.Error with structured fields
    for Logging and Recovery
  - internal/mcp/server.go uses slog instead of log
  - internal/mesh/handler.go uses sloig instead of log
  - internal/registry/handler.go uses slog instead of log
  - internal/registry/postgres_store.go uses slog instead of log
  - go build ./... && go vet ./... && go test ./... -short all pass
  - No log.Printf, log.Println, or log.Fatalf remain in source files (test files exempt)
  status: complete
  created_at: '2026-07-19T18:21:27.081751+00:00'
  completed_at: '2026-07-19T18:23:57.702753+00:00'
- id: bug-001-ws-hijacker
  title: 'BUG-001: WebSocket upgrade returns 500 through middleware chain (missing
    http.Hijacker)'
  criteria:
  - responseWriter in internal/middleware/middleware.go implements http.Hijacker (Hijack()
    method delegating to the underlying writer)
  - Regression test in internal/middleware/ exercises a WebSocket upgrade + echo round-trip
    through the full middleware chain (Recovery + Logging)
  - go build ./... && go vet ./... && go test ./... all pass
  - 'Live E2E: WebSocket subscribe on a running server returns 101 and receives published
    events'
  status: pending
  created_at: '2026-08-01T04:19:39.148381+00:00'
- id: ci-012-sigterm-flake
  title: "CI-012: cmd/server TestServerHealth intermittent 'signal: terminated' \u2014\
    \ SIGTERM handler registration race"
  criteria:
  - signal.Notify(SIGINT, SIGTERM) is registered synchronously in main() BEFORE ListenAndServe
    (not inside the goroutine)
  - TestServerHealth passes 20/20 consecutive runs
  - go build ./... && go vet ./... && go test ./... all pass
  status: complete
  created_at: '2026-08-01T04:19:45.678263+00:00'
  completed_at: '2026-08-01T04:29:04.140049+00:00'
