# Example task for a REAL opencode run against this stack (no-key CI runs
# fall back to the canned reply; with DEEPSEEK_API_KEY set, `opencode run`
# executes this task and posts the result through crier).

## Task
Explain, in exactly two sentences, what a message bus is. Do not use tools.
